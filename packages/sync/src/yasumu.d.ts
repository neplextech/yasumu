declare const Yasumu: {
  ui: {
    showConfirmationDialogSync(options: {
      title: string;
      message: string;
      yesLabel: string;
      noLabel: string;
      cancelLabel: string;
    }): boolean;
  };
};
